version: "3.8"

services:
  # db:
  #   image: postgres
  #   restart: always
  #   environment:
  #     POSTGRES_USER: root
  #     POSTGRES_PASSWORD: password
  #     POSTGRES_DB: test_db
  #   ports:
  #     - "5435:5432"
  #   networks:
  #     - app-network

  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - app-network
    depends_on: [] 

networks:
  app-network:
    driver: bridge
